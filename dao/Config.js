/**
 * auth:黄国烨
 * miai:448249687@qq.com
 *desc:数据库注册信息。
 * */
/*
* //Module.exports指将文件暴露给module空间，而文件所代表的为当前函数。（此文件函数为json格式）。
 * host:为mysql的url，user，password，database，ports分别为：
 * 需要用的数据库的帐号、密码、数据库名和端口号。
* */
module.exports={
    host:'localhost',
    user:'root',
    password:'root',
    database:'node01',
    ports:3306
};
